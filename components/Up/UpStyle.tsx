import { cva } from 'class-variance-authority'

export const cvaUp = cva([
	'h-10 w-10 bg-primary rounded-[10px] shadow-md flex items-center justify-center bottom-[30px] right-[30px] fixed border-none cursor-pointer hover:bg-primary-hover'
])
